#include <bits/stdc++.h>

using namespace std;

class Solution {
public:
    long long countQuadruplets(vector<int>& nums) {
        // A quadruplet (i, j, k, l) is increasing if:
        // 0 <= i < j < k < l < n, and
        // nums[i] < nums[k] < nums[j] < nums[l].

// write in efficent way without using sort

        
    }
};